struct Settings {
  float zoomStart;
  float zoomCount;
  float pointHeight;
  float lineHeight;
  float areaHeight;
  float areaBorderHeight; 
  float spriteMetaHeight;
  float spriteHeight;
  float spriteStart;
  float featureCount;
};

#pragma glslify: export(Settings)
